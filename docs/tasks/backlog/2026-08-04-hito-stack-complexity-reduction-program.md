# Hito Stack Complexity Reduction Program

## Work Item ID

2026-08-04-hito-stack-complexity-reduction-program

## Status

in_progress

## Type

plan

## Priority

medium

## Owner

product

## Scope

cross-stack-simplification

## Batch

hito-stack-simplification

## Archive Intent

retain_in_place

## Task

Reduce demonstrated Hito Running complexity without changing the retained product stack, creating a
second truth path, or treating line count as proof of a defect. This is the canonical operational
record for cross-owner simplification work; each executable slice must remain separately owned and
evidence-gated.

## Stage

Slices 8A and 8B are released at `0630edd` and `df796cc`; Slice 8C has passed its
Design System implementation and independent owner-level QA. Slice 8D is the next Design System
boundary after the exact 8C release. The concurrent FIT Frontend presentation remains separate and
preserved. The parent stays `in_progress` until every admitted slice closes or is retained with
current evidence.

## Next Recommended Role

design_system

## Exact Handoff Prompt

```text
ROLE: DESIGN SYSTEM

Task:
Consolidate duplicated generic Hito DS workbench settings controls and make the evergreen foundation
proof reachable through the canonical DS validator.

Stage:
DESIGN SYSTEM implementation with integrated QA, Slice 8D.

Required outcome:
- Establish one parity-proven workbench settings owner used by Calendar and Workout Library.
- Make foundation cleanup assertions package-reachable through the canonical DS validation owner.
- Delete duplicate local controls and the redundant proof root after exact behavior/assertion parity.
- Preserve Calendar geometry, the 32 workout identities, public /hitoDS, manifests, focus, width, and
  accessibility; do not create a generic workbench framework or parallel validator.

Approval policy: Routine local implementation, independent QA, exact-manifest release, push,
deployment, and smoke validation proceed under standing authorization. Do not mutate hosted data or
interrupt another active role.
```

## Completed Slice 6 Handoff Record

```text
ROLE: DESIGN SYSTEM

Task:
Align `/hitoDS` with its accepted role as a production-shipped, interactive Design System reference
surface, and admit only source-proven representation simplification.

Evidence before code:
Slices 1-5 are complete. Product has decided that any Hito user may browse and interact with the
showcase as a visual reference and sandbox. The route must render the same canonical tokens,
typography, primitives, and component contracts used by Product, while remaining isolated from
Product data, business lifecycle, and state. Existing route metadata and supporting documentation
still describe the shipped route inconsistently, and TypeScript/JSON manifest representations have
distinct known consumers.

Required outcome:
- Make the route's metadata, Hub/reference language, and Design System ownership consistent with its
  accepted production reference role.
- Prove that the showcase consumes canonical Design System primitives and tokens rather than a
  separate visual system, and that ordinary Product routes do not depend on the showcase.
- Consolidate a manifest representation only when current runtime and validator consumers have exact
  replacement proof; otherwise retain the separate representation with its factual owner and reason.
- Complete the full owner-scoped implementation, QA, and release loop for every admitted change.
- Use up to six active, non-nested subagents for genuinely independent token/component provenance,
  route isolation, runtime, browser, security, or release evidence; integrate and close each result.
- Maintain this canonical backlog item directly: keep the admitted slice truthful while active and
  record its exact outcome before your final report.

Boundaries:
- Do not remove `/hitoDS` from production reach, change current route access, introduce Product data
  or mutation behavior, create a second token/component truth, or redesign runner-facing Product UI.
- Do not reopen the released DTO/comparison cutover, Backend cleanup, or unrelated build/runtime work.

Definition of Done:
The public showcase is truthfully described and isolated, uses canonical tokens/primitives, preserves
current reference and validator behavior, and contains no admitted duplicate representation without
consumer proof. Required source, build, runtime, browser, and independent Design System QA evidence
passes for every changed contract. The exact release manifest is committed, pushed, deployed, and
production-smoked without including concurrent work.

Approval policy: Routine local inspection, implementation, fixture QA, validation, exact-manifest
release integration, commit, push, deployment, and safe production smoke are authorized. Do not
mutate hosted data/schema or call providers without a demonstrated need; do not print credentials or
private data.
```

## Completed Slice 5 Backend Handoff Record

```text
ROLE: BACKEND

Task:
Implement the Backend-owned half of the coordinated Progress/generated-preview DTO and comparison
cutover in the canonical Hito Stack Complexity Reduction Program. This is one atomic cross-owner
release boundary with Frontend Product; do not release or represent the Backend half as complete on
its own.

Evidence before code:
Backend runtime/proof cleanup, Lovable removal, and the admitted proof/build helper cleanup are
released through 809644d3af98fcb17ab2b58f807241692603e0bb. The Progress endpoint still exposes its
internal read model, generated preview still exposes the full reviewed draft, comparison new writes
and persisted readback still require both `signals` and `facts`, and Frontend Product still has a
live `facts` fallback. The accepted read-only inventory found one historical comparison row with an
exact canonical dual representation and no facts-only or contradictory row. Recheck current source,
consumer, and working-tree evidence before editing.

Required outcome:
- Establish explicit Product-facing Backend projections at the existing Progress and generated-plan
  preview response owners while retaining canonical lineage, review, confirm, persistence, and
  observability truth inside their existing owners.
- Make the existing comparison boundary deliver one canonical `signals` representation to Product,
  preserve accepted historical readback without hosted data rewriting, and reject contradictory
  evidence rather than selecting a second truth.
- Preserve truthful Progress `current`, `updating`, and unavailable states, runner-safe errors,
  review-token/checksum exactness, normalized preview input, calendar/workout readback, privacy/RLS,
  and provider isolation.
- Produce the Backend contract and validation receipt needed by the paired Frontend Product owner
  and one exact shared release manifest. Complete Backend validation and one independent read-only
  review inside this task, then keep the task open until the paired consumer and integrated release
  gates pass.

Boundaries:
- Do not edit Frontend Product or Design System owners, change Product presentation, schemas,
  migrations, formulas, fixtures, provider behavior, hosted data, or accepted release history.
- Do not narrow a live public contract independently, create another DTO/read model, retain
  `signals` and `facts` as parallel Product truths, or make the review token carry less exactness.
- Stop for a genuine Product decision if current Product behavior requires a field or semantic state
  that cannot be derived from accepted consumer evidence. Otherwise coordinate the contract with
  Frontend Product and fix forward inside this atomic boundary.

Definition of Done:
Backend no longer serializes the targeted internal models as accidental Product contracts; the
comparison Product boundary has one `signals` representation with safe historical compatibility;
privacy, persisted readback, signed review/confirm, generated-preview non-persistence, truthful
Progress states, payload/query measurements, targeted validators, build/integrity, and fresh built
loopback runtime checks pass. The final report must list every required check in
`Check | Scenario / environment | Result | Evidence`, integrate one independent reviewer, identify
the exact paired Frontend contract, and remain open until both owner halves and the integrated
cross-owner release candidate pass without a partial release.

Approval policy: Routine local inspection, owner-scoped implementation, local fixture/runtime
control, and validation proceed under standing authorization. Do not stage, commit, push, deploy,
mutate hosted data, or call providers without explicit authority.
```

## Completed Slice 5 Frontend Product Handoff Record

```text
ROLE: FRONTEND
Frontend lane: Product

Task:
Implement the Frontend Product-owned half of the coordinated Progress/generated-preview DTO and
comparison cutover in the canonical Hito Stack Complexity Reduction Program. This is one atomic
cross-owner release boundary with Backend; do not release or represent the Frontend half as complete
on its own.

Evidence before code:
The released baseline is 809644d3af98fcb17ab2b58f807241692603e0bb. Product Progress currently
imports the Backend Runner Activity read model directly, generated-plan preview consumes the full
reviewed draft including its canonical plan, and workout comparison still falls back from `signals`
to `facts`. Backend owns the paired projections, canonical comparison compatibility, privacy, and
response semantics. Recheck the live contract, consumer graph, and working tree before editing.

Required outcome:
- Consume the agreed explicit Product DTOs for Progress and generated-plan preview without creating
  a Frontend-owned copy of canonical activity, plan, comparison, or persistence truth.
- Consume one canonical comparison `signals` representation and remove the live `facts` fallback
  only in the same accepted release candidate as the paired Backend contract.
- Preserve runner-visible Progress, generated-preview, review, explicit-confirm, comparison, error,
  retry, and refresh behavior. Truthful `updating` must lead to a bounded refetch/readback outcome or
  remain explicitly unavailable; it must not become a false ready state.
- Preserve the existing Product presentation and accessibility contract; this is contract adoption,
  not a redesign or another Hito DS control migration.
- Complete Frontend validation and one independent QA review inside this task, then keep the task
  open until the Backend half and integrated release gates pass.

Boundaries:
- Do not edit Backend, schema, migration, formula, fixture-truth, provider, Design System, `/hitoDS`,
  deployment, or hosted-data owners.
- Do not keep compatibility aliases or a second consumer model after the accepted cutover, remove a
  fallback before the paired Backend shape exists, or infer API adoption from earlier Hito DS
  control work.
- Stop for a genuine Product decision if the agreed DTO would change visible meaning, copy, user
  control, or review/confirm semantics. Otherwise fix forward with Backend inside the atomic boundary.

Definition of Done:
Progress, generated preview, and comparison consumers use the agreed Product contracts; no live
`facts` fallback or direct targeted internal-model import remains; current/updating/unavailable,
review/confirm, retry/readback, and historical comparison behavior remain truthful. Targeted type,
lint, build/integrity, fresh built-loopback, and representative browser checks pass at desktop and
375px in light/dark mode, including keyboard/focus and failure/retry states. The final report must
list every required check in `Check | Scenario / environment | Result | Evidence`, integrate one
independent QA reviewer, name the exact paired Backend contract, and remain open until both owner
halves and the integrated cross-owner release candidate pass without a partial release.

Approval policy: Routine local inspection, owner-scoped implementation, local fixture/runtime and
browser control, and validation proceed under standing authorization. Do not stage, commit, push,
deploy, mutate hosted data, or call providers without explicit authority.
```

## Target Stack

Retain one React/TanStack Start/Vite application, Supabase Auth/Postgres/Storage, Tailwind with Hito
DS and Radix, Zod, generated Supabase types, Nitro, and Vercel. Reject a framework migration,
microservices, GraphQL, a second state manager, a second backend, and a second product-data truth
unless a future measured requirement establishes a need.

## Current Evidence And Admission Rules

- Complexity is concentrated in broad owners, build/runtime coordination, proof tooling, and
  duplicated operational documentation, not in an excessive number of product frameworks.
- Released source-control truth is `809644d3af98fcb17ab2b58f807241692603e0bb`; local `main` and
  `origin/main` match, and `79d5c85...` plus `cca4f7c...` are ancestors of both. The working tree is
  not a release manifest: it contains this Architecture reconciliation and separate concurrent
  policy/runtime work. The earlier `dec2e22...` size measurement remains historical audit evidence.
- The application has one reachable framework chain: React -> TanStack Start/Router -> Vite ->
  Nitro -> Vercel, with Supabase, Tailwind/Radix, and Zod. No second application framework,
  backend, ORM, state manager, or test framework is present.
- Lovable wrapper convergence is released at `cca4f7c...`: the wrapper and its package/lock residue
  have zero current repository reachability, while direct Vite/TanStack/React/Tailwind/path
  configuration and deployment-parity proof are retained. Nitro remains canonical and independently
  owns optional Miniflare/Wrangler/Workerd dependencies, so the release never claimed removal of all
  Cloudflare-related packages or advisories.
- A zero direct-import count is not deletion proof for compiler, generated-type, or wrapper-owned
  packages. Direct Vite/TanStack/React/Tailwind/path plugins remain intentional inputs to the
  released direct configuration after wrapper removal.
- Proof sprawl is custom code, not an extra test framework. The released Backend manifest and shared
  helpers are tracked canonical owners; `809644d...` additionally retired the zero-consumer
  `qa:server` alias, routed proof-local account paths to the canonical registry constant, and reused
  one PID-liveness owner. No second test framework was introduced.
- The legacy source-size plan is supporting history, not an operational queue. Its claim that
  `use-mobile.tsx` is unconsumed is stale: current Product consumers include onboarding and Progress.
- The initial zero-consumer review is closed: `IntervalsViz`, `weeklyMileage`, `statsTotals`,
  `@tanstack/react-query`, and `@radix-ui/react-progress` were removed with source, lockfile,
  build, runtime, and independent-QA proof. `date-fns` remains only as the required transitive
  dependency of `react-day-picker`.
- Large modules such as `training.ts`, `ManualWorkoutConstructorEditor.tsx`, `CompletionPanel.tsx`,
  and `workout.$date.tsx` require a demonstrated state, lifecycle, or ownership boundary before any
  extraction. The Frontend owner-complete Hito DS Product control migration changed shared UI
  control adoption only; its separate Global QA remains pending, and it did not change these
  modules' Backend/API ownership.
- iCloud checkout and custom Vite/Nitro workarounds require a measured build/runtime discriminator;
  do not replace or remove them from a source-only audit.

## Complexity Budget And Retirement Rule

- Reuse a reachable canonical owner before adding a dependency, helper, validator, fixture,
  generated representation, or operational document.
- Every implementation slice must name what becomes smaller or disappears. A new permanent layer is
  admitted only when a measured requirement cannot be met by an existing owner.
- Count product runtime, generated code, proof tooling, and retained evidence separately. Line count
  identifies an audit target; it never proves dead code or a defect.
- Keep operation-specific safety contracts separate. Shared serialization, metadata, fixture,
  runtime, and lifecycle plumbing belongs in the existing shared owner only after consumer parity is
  proven.
- Do not trade away persistence readback, privacy/RLS, cleanup, browser behavior, release parity, or
  historical evidence to improve a size metric.

## Verified Reuse And Retirement Inventory

| Surface                 | Verified owner or finding                                                                                                                                                                                                 | Decision                                                                                                                                                                                                          |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Backend validation      | `scripts/validate-backend.mjs` and its shared runtime, test-user, persistence, Runner Activity, and generated-plan helpers are tracked in release `79d5c85...`.                                                           | **Released:** canonical receipt and live manifest agree on 13 default, 16 local-db, 15 runtime, and 16 release checks. Preserve this surface; do not reopen the accepted cleanup without a failing discriminator. |
| QA server commands      | `809644d...` removed the zero-consumer bare `qa:server` alias; explicit status/start/restart/stop commands remain canonical.                                                                                              | **Released:** preserve the explicit lifecycle commands and do not recreate the ambiguous alias.                                                                                                                   |
| Local account fixtures  | `LOCAL_AUTH_BYPASS_ACCOUNTS_FILE` remains the live environment contract; `809644d...` replaced admitted proof-local fixture literals with the canonical default registry constant.                                        | **Released for admitted paths:** preserve one account-registry owner. Any newly found literal needs fresh execution-path proof and a bounded owner before removal.                                                |
| Manual Workout metadata | The accepted Backend closure found similar JSON and mutation-metadata helpers but different lifecycle inputs and write semantics.                                                                                         | **Retained, not admitted:** do not create a generic Manual Workout abstraction. Reopen only if new behavior-level parity proves one shared owner and deletes the old paths in the same slice.                     |
| Build lifecycle         | `809644d...` reused the existing build-lock PID-liveness owner in QA server control. Required iCloud/Nitro finalization remains live; `scripts/finalize-build-output.mjs` has separate concurrent work outside this task. | **Released for the admitted duplicate:** preserve the finalization lifecycle and do not absorb or infer acceptance of the concurrent output-path change.                                                          |
| Hito DS manifests       | Code-owned TypeScript and JSON representations still have distinct runtime/validator consumers. Product has retained `/hitoDS` as a production-shipped interactive reference surface.                                     | **Admitted to Design System:** align public-reference metadata and prove consumer parity before any representation convergence; retain each representation that still has a distinct consumer.                    |
| Orphan proofs           | Two unique Frontend Product proofs and one tooling artifact-hygiene proof have no package, import, or current-doc entrypoint.                                                                                             | Retain and assign them to their existing Frontend Product and Architecture/tooling validation owners before any move or deletion.                                                                                 |
| `/hitoDS` and DevTools  | DevTools is lazy and loopback-gated; `/hitoDS` is statically shipped and linked from the Hub. Product has accepted it as a production-shipped interactive Design System reference.                                        | Keep the showcase production-reachable and independent of Product data/lifecycle; admit only source-proven metadata or representation cleanup under Design System ownership.                                      |

## Coordinated DTO And Comparison Cutover

- The existing Backend backlog item remains `blocked` only for the program's next executable boundary:
  Progress and generated-preview explicit Product DTOs plus removal of the comparison `facts`
  writer/type/readback fallback in favor of canonical `signals`.
- Backend owns projections, persistence/writer normalization, privacy, and response contracts.
  Frontend Product owns consumer types, rendering/readback, retry/refresh behavior, and removal of
  the live `facts` fallback. Neither owner may cut the contract over independently.
- The Frontend owner-complete
  [Hito DS Product control adoption](2026-08-04-hito-ds-product-control-adoption.md) changed Button,
  Field, and Choice ownership only; its separate Global QA remains pending. It does not satisfy,
  partially satisfy, or unblock API DTO/comparison consumer adoption.
- This coordinated dependency remains in the existing Backend backlog record. It does not create a
  third plan, queue, DTO truth, or comparison truth.
- Backend and Frontend Product may implement only their own owner-scoped halves, but neither half is
  accepted or releaseable alone. One release candidate must contain both halves, an exact shared
  manifest, matched contract/consumer types, safe historical comparison readback, and the integrated
  validation matrix below:
  - Progress GET and both activity-mutation response keysets on representative and 3,000-activity
    fixtures, Backend privacy/RLS, payload/query measurements, and `updating` to eventual Product
    refetch/readback without exposing internal lineage;
  - generated-preview ready/unavailable/cancel/refresh behavior in onboarding and active-plan
    creation, signed review/confirm exactness, no persistence before confirm, and provider isolation;
  - comparison signals-only new-write readback, accepted exact-dual historical normalization,
    facts-only/contradictory/duplicate rejection, carry-forward identity, upload and workout-loader
    readback, Product rendering, and AI-insight association;
  - representative desktop and exact 375px browser proof in light/dark mode, including error,
    unavailable, updating, keyboard, and focus states;
  - one independent cross-owner review, scoped lint/format/diff hygiene, production build/integrity,
    and no partial source-control release.
- A same-slice field or semantic conflict that changes visible Product meaning is the only Product
  decision stop. Technical ownership or sequencing uncertainty is not permission to ship one half.

## Slice 5 Closure Receipt

- Backend now projects explicit Product DTOs at the existing Activity History, factual Progress,
  activity-mutation, and generated-preview response owners; canonical lineage and reviewed-plan truth
  remain internal to their existing owners.
- Product consumers use those DTOs directly. Generated preview no longer receives the full canonical
  reviewed draft, Progress no longer imports the internal read model, and workout comparison has no
  live `facts` fallback.
- New comparison writes and Product readback use `signals` only. The one evidenced exact historical
  dual representation normalizes to `signals`; facts-only, contradictory, duplicate, malformed, and
  extra-key forms reject without repair or hosted-data rewriting.
- Four-distance provider-shaped preview, signed review/confirm, local persistence/RLS, 30- and
  3,000-activity read models, Gate 4, comparison carry-forward, payload/privacy, production build,
  Vercel parity, built loopback runtime, desktop and exact 375px light/dark Product QA, keyboard/focus,
  failure/retry, and independent source review passed. Provider calls and hosted data/schema mutations
  were neither required nor performed.
- The exact atomic release excludes concurrent `AGENTS.md` and build-finalizer work. Global QA remains
  separate; this receipt closes only the Slice 5 owner-level and integrated release boundary.

## Slice 6 Closure Receipt

- The scoped implementation commit is `96ce0d6`. Its first detached clean-checkout replay exposed a
  pre-existing validator dependency on an empty Git-untracked `scripts/fixtures` directory; the
  release-boundary follow-up containing this corrected receipt deletes that zero-source scan root and
  repeats the entire clean-checkout gate. No compatibility directory or hidden fixture dependency is
  introduced.
- The exact Slice 6 release candidate is based on accepted `origin/main`
  `88dc74914a66d27a2a0a2910a9fb7353ea3cec3f`. That concurrent Backend release changed none of the
  ten Design System manifest paths, so the completed independent DS review remains source-equivalent;
  unrelated dirty `AGENTS.md` and build-finalizer work stay outside this release.
- `/hitoDS`, its Hub entry, the live reference language, current Product/System/State documentation,
  and Design System ownership now describe one production-shipped public interactive reference and
  sandbox. Route access is unchanged and no auth gate was added or removed.
- The showcase consumes canonical foundations, shared typography provenance, and existing
  `src/components/ui/*` primitives. Semantic gradient previews now render the canonical canvas,
  auth-overlay, and editorial classes rather than maintaining a second hardcoded visual recipe.
- `validate-hito-ds-component-contracts.ts` now protects the inverse dependency boundary: ordinary
  Product source cannot import showcase modules directly, relatively, or through `/hitoDS` route
  re-exports; Local Inspector retains only its exact read-only `reference-metadata` provenance seam.
- TypeScript and JSON manifests remain generated projections of one source digest. The TypeScript
  artifact has three live reference/capture-board consumers, while JSON remains the language-neutral
  foundation validator/downstream artifact; representation deletion was not admitted without an
  exact replacement proof.
- The globally loaded `reference-workbench.css` remains intentionally intact and is documented as a
  mixed Design System owner because shared icon, logo, surface, divider, and workbench selectors have
  Product consumers. A file split without a selector migration would be cosmetic duplication, not
  simplification.
- Component-contract, foundation-retirement, manifest-parity, lint, formatting, diff, local runtime,
  desktop `1470x801`, exact `375x812`, light/dark, navigation, focus/Escape, interactive Slider,
  overflow, console, network, and independent Design System QA checks passed. The atomic release
  commit containing this receipt must also pass clean-commit build/integrity and production smoke
  before the owner reports release closure.
- No Product data, provider, hosted schema/data, persistence, auth, Calendar behavior, or
  runner-facing UI changed. Safari was not required because no cross-engine primitive behavior
  changed; Global QA remains separate and pending.

## Slice 8C Closure Receipt

- [Neutral Hito selection mechanics](2026-08-05-hito-ds-neutral-selection-mechanics.md) is completed.
- One internal owner now provides the shared item shape, safe-ID normalization, enabled tab stop,
  and wrap movement; Tabs and Radio retain separate roles, ARIA, IDs, key maps, focus, activation,
  and controlled-state contracts.
- Duplicate blocks, the Radio-to-Tabs type import, and one dead reference import are removed.
- Canonical validators, Product and /hitoDS pointer/keyboard/ARIA checks, desktop and exact 375px
  light/dark proof, build/integrity/runtime, ARCHITECT review, and independent QA passed.
- Global QA remains separate and pending.

## Owner-Scoped Roadmap

| Slice                                    | Owner                         | Operational state and outcome                                                                                                                                                                                                                                                      | Required proof                                                                                                                                                                                                                | Stop condition                                                                                                     |
| ---------------------------------------- | ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| 1. Frontend residue                      | Frontend Product              | **Completed:** removed `IntervalsViz`, two unused `training.ts` exports, and two direct unused dependencies. `date-fns` remains only as the required transitive dependency of `react-day-picker`.                                                                                  | Consumer graph, dependency reverse scan, lint, build/integrity, loopback runtime/browser smoke, and independent QA passed.                                                                                                    | Closed without touching live consumers or concurrent Product work.                                                 |
| 2. Backend runtime and proof convergence | Backend                       | **Released at `79d5c85...`:** npm-only toolchain, one Backend validation manifest, shared proof/runtime helpers, local-account registry, response narrowing, source cleanup, privacy/RLS hardening, and Backend-only comparison normalization.                                     | Commit ancestry/parity, canonical 16 local-db / 15 runtime receipt, build/integrity, hosted parity, independent QA, and tracked-helper reachability passed.                                                                   | Closed. The coordinated DTO/comparison cutover does not reopen this release.                                       |
| 3. Lovable wrapper convergence           | Backend, tooling/build scope  | **Released at `cca4f7c...`:** direct retained Vite/TanStack/React/Tailwind/path configuration replaced the managed wrapper and removed wrapper-owned dependency residue.                                                                                                           | Four-path manifest, dependency/lockfile provenance, development, production build/integrity, built loopback runtime, deployment parity, and independent review passed.                                                        | Closed. Nitro's separately owned optional Cloudflare graph remains intentional.                                    |
| 4. Proof/build helper retirement         | Backend with Architect review | **Released at `809644d...`:** retired `qa:server`, canonicalized admitted proof account paths, and reused the build-lock PID owner without removing the iCloud/Nitro lifecycle.                                                                                                    | Nine-path manifest, zero-consumer and replacement proof, targeted validators, build/integrity, runtime parity, exact index/remote parity, and independent review passed.                                                      | Closed. Concurrent `finalize-build-output.mjs` work and any unproven helper candidate remain outside this receipt. |
| 5. Coordinated DTO/comparison cutover    | Frontend Product + Backend    | **Released by the atomic commit containing this receipt:** explicit Product DTOs replace targeted internal serialization, and Product uses one `signals` comparison representation with strict historical normalization.                                                           | Shared 22-path manifest; Backend privacy/response/persistence/review proof; Frontend type/render/retry proof; historical compatibility; build/integrity; built runtime; browser QA; Vercel parity; independent review passed. | Closed without server-only narrowing, Frontend-only fallback removal, hosted data rewrite, or partial release.     |
| 6. `/hitoDS` public reference alignment  | Design System                 | **Completed by the release boundary ending at the commit containing this receipt:** public metadata and language align with the shipped sandbox; canonical provenance and Product isolation are validator-protected; both distinct generated representations remain parity-proven. | Metadata/reference alignment, token/component provenance, inverse dependency guard, route isolation, manifest parity, build/integrity, desktop/375 light/dark browser proof, production smoke, and independent DS QA passed.  | Closed without access changes, Product-data coupling, a second visual truth, or unproven manifest deletion.        |
| 7. Current-truth compression             | Product/docs with Architect   | **Later documentation hygiene:** after executable status stabilizes, reduce overlapping current documentation while keeping this backlog as the sole operational queue and completed receipts discoverable.                                                                        | Backlog/status inventory, source-of-truth claim scan, inbound-link preservation, local-link validation, formatting, and scoped diff hygiene.                                                                                  | An active item, decision, release receipt, or useful historical evidence would lose discoverability or ownership.  |

## Existing Supporting Records

- [Source-size governance and cleanup plan](../../plans/active/2026-06-30-hito-source-size-governance-and-cleanup-plan.md) remains retained supporting history; it is not an execution queue.
- [Runner Activity Backend Optimization Plan](2026-08-03-runner-activity-backend-simplification-and-metric-scalability.md) remains the separate Backend owner for accepted Runner Activity lifecycle optimization.
- [Backend runtime contract and proof simplification](2026-08-04-backend-runtime-contract-and-proof-simplification.md) records the released Backend boundary at `79d5c85...`; the coordinated DTO/comparison dependency is closed by Slice 5 here without duplicating or absorbing that Backend contract owner.

## Preserved Boundaries

Do not reopen accepted Runner Activity Gates 1-4, Backend runtime/proof release `79d5c85...`,
Lovable release `cca4f7c...`, proof/build helper release `809644d...`, Slice 5 DTO/comparison release,
planned-workout projection reconciliation, FIT ingestion truth, metric provenance, RLS/privacy,
fixture isolation, or the generated-plan preview/review/confirm lifecycle. Do not represent the
completed Frontend Product control migration as API-consumer adoption. Do not delete dependencies,
exports, files, validators, fixture paths, or compatibility behavior from size, age, or a stale
document claim alone.

## Next Admission

Slices 8A and 8B are released at `0630edd` and `df796cc`; Slice 8C has passed owner-level
implementation and QA and proceeds through its exact release before Slice 8D begins. Slice 8D is the
only next executable boundary. The parent remains `in_progress`; no mixed-owner release, broad
rewrite, framework migration, new queue, or deletion from line count is admitted.
