# Developer Velocity Patch Pack And Fast Visual Lane

## Work Item ID

2026-08-06-developer-velocity-patch-pack-fast-visual-lane

## Status

completed

## Type

change_request

## Priority

high

## Owner

architect

## Scope

docs-and-source-of-truth

## Archive Intent

retain_in_place

## Task

Establish one reusable Patch Pack and Fast Visual Lane operating contract for small Hito Product and
Design System work, using the existing functional map, managed QA lifecycle, direct Vite iteration,
validators, and backlog rather than adding a second workflow system.

## Stage

ARCHITECT delivery-policy implementation and independent review completed. The parent program is
backlog until an existing evidence-backed Product or Design System item qualifies as the first real
pilot. No Product, Design System, Backend, browser, or runtime implementation is included.

## Historical Handoff Prompt

```text
ROLE: ARCHITECT

Task:
Implement Developer Velocity Slice 2: establish the canonical Patch Pack and Fast Visual Lane
operating contract for small Hito Product and Design System changes.

Stage:
ARCHITECT documentation and delivery-policy implementation with independent review.

Required outcome:
Define one same-owner Patch Pack convention and a Fast Visual Lane that uses current route-owner
navigation, direct iteration, stable local proof reuse, proportional final verification, and one
build/integrity gate only when the completed visual contract reaches it. Do not create a second task
queue, runtime, framework, validator, or Inspector persistence feature.

Boundaries:
Preserve Product behavior, shared contracts, provider mode, fixtures, release semantics, and the
blocked FIT browser-evidence work. Touch only the exact policy and canonical Velocity records; do not
absorb concurrent AGENTS or documentation hunks. Do not stage, commit, push, deploy, mutate hosted
state, or call providers.

Definition of Done:
The policy gives a Frontend Product or Design System owner one unambiguous same-surface fast path,
one split/escalation rule for higher-risk work, and one truthful final proof boundary. The first real
Product pilot remains unselected until an existing evidence-backed item qualifies. Global QA
Acceptance is not claimed.

Approval policy:
Routine local inspection, documentation edits, independent review, and validation proceed under
standing authorization. Do not request routine approval. Do not stage, commit, push, deploy, mutate
hosted state, or call providers.
```

## Parent

[Developer Velocity And Proportional Verification](2026-08-05-developer-velocity-and-proportional-verification.md)

## Execution Preflight

- Evidence: Backend Slice 1 is completed at `4568f04`; healthy unchanged artifact reuse is now
  fast and executable-input drift fails closed. Remaining measured friction is procedural
  ownership/proof selection for small visual work, not a Vite performance defect.
- Canonical owner: the parent Velocity item and `AGENTS.md`; the functional map remains the route
  and verifier index.
- Outcome: one same-owner, same-risk visual Pack has a narrow iteration and final-proof boundary;
  broad rediscovery, repeated setup, approval waiting, and per-adjustment rebuilds are not required.
- Proof: canonical metadata/importer dry-run, map/policy consistency, local links, formatting,
  scoped diff hygiene, and independent review.
- Stop: split work at shared DS, backend/DTO, persistence/auth/FIT/provider, release, Product
  decision, or incompatible browser/fixture boundary; do not turn the policy into a Product change.

## Evidence And Constraints

- The functional map already resolves route -> presentation owner -> truth owner -> first verifier.
- The managed QA lifecycle is the final browser/runtime path; direct Vite/HMR remains iteration only.
- The uncommitted FIT Product presentation remains blocked on native attachment, upload, and
  readback proof. It is not a Fast Visual Lane candidate.
- No ready evidence-backed Product visual Pack exists today. The next qualifying existing Product
  item, not a fabricated cosmetic task, is the pilot.

## Closure Receipt

- `AGENTS.md` section 2.59 now owns the sole Patch Pack and Fast Visual Lane contract. It preserves
  section 5.5 individual-bug lifecycle, section 6.5 browser/runtime policy, and the existing
  functional map/validator owners.
- `design_system` is now a valid `Next Recommended Role`, allowing a shared-DS Pack to remain
  accurately represented without assigning it to a Frontend lane.
- The parent records Backend Slice 1 completion at `4568f04` and this policy Slice 2 completion.
  No Product pilot, runtime acceptance, or Global QA Acceptance is claimed.
- Two independent read-only reviews passed after fix-forward. No concurrent Product, FIT, finalizer,
  or unrelated AGENTS lifecycle hunk was absorbed.
- The technical log is unchanged: this is an operational-policy source-of-truth update, not an
  accepted runtime, local-tooling, or Product implementation release.
