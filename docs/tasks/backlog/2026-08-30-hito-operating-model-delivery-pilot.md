# HITO-302 — Prove Hito Operating Model Through One Delivery Pilot

- Work Item ID: `HITO-302`
- Status: Done
- Type: Maintenance
- Priority: Highest
- Owner: QA
- Primary Area: Platform
- Epic: `recover-hito-delivery-operating-model`
- Lifecycle: [Live Notion Task](https://app.notion.com/p/3ccfe5f58cf581ad9db7cecd5ba98a4b)
- Archive Intent: retain the single pilot lineage, measurements, release proof and cleanup receipt.

## Task

Prove one newly admitted bounded fixture-safe Task from intake through implementation, independent QA
and Git-backed release using the recovered operating model.

## User Report

Source/build claims have repeatedly been reported as complete while the actual user journey or
accepted artifact was absent, stale or unbound.

## Evidence And Observed Behavior

HITO-292 provides the baseline: late capability failure, hidden approval, wrong cwd, stale overlay,
repeated context and Ivan relay.

## Expected Behavior

One manifest lineage survives every acknowledged owner transition. Source motion invalidates QA;
one writer owns each stage; disposable runtime and data are cleaned; release evidence is exact.

## Source Investigation And Root Cause

The new process is not accepted by documentation or unit proof. Only a real bounded pilot can prove
that the replaced orchestration behavior is gone.

## What Not To Touch

Do not reopen HITO-292, use personal sessions, select a paid/provider/data-sensitive journey or
expand the pilot after admission.

## Validation Expectations

Record handoff time, repeated context, capability failures, cwd/source/artifact/runtime identity,
human relay count, release revision and exact cleanup against the baseline.

## Admitted Pilot

Remove only the stale `.prettierignore` entry `pnpm-lock.yaml`. HITO-300 proved npm and
`package-lock.json` are the sole package-manager truth and recorded this exact line as a recoverable
DELETE-CANDIDATE. The implementation source allowlist is `.prettierignore` only. The expected diff
deletes exactly that one line and preserves every other byte.

Rollback restores `pnpm-lock.yaml` at its prior position. No product source, runtime, fixture data,
dependency, lockfile, hosted service, secret or deployment is admitted. After independent QA accepts
one frozen manifest lineage, the Task requires one Git-backed commit and push; release ownership and
authority do not begin before that acceptance.

## Accepted Result — 2026-08-30

The pilot removed only the stale `.prettierignore` entry and released commit
`28cddb8f90b094c66eacae3d9a903a0889336b2b` to `origin/main`, with parent
`fca161507e9dd344a141712d48d799b4204091d8`. Independent QA accepted post-source SHA-256
`23ddbc30d63f00c7a6f5d53454210eea2cecbdc7de35f34e1121531c055b95ad` and final release manifest
`d24ae35ec9d3b4457a99d32fde871b95aaf03cab3b0591751d319c419cef560c`.

The pilot exposed and fixed two adjacent broker roots on the same Task before acceptance: post-edit
source identity and post-release repository identity. The accepted single verifier now keeps
pre-operation admission immutable, seals exact post-operation admitted-path state, and for
`git_release_deploy` additionally binds commit, parent, remote revision, branch, index,
unrelated-dirty fingerprint and changed-path set. Non-Git base motion remains fail-closed.

All source, QA and release leases were released; retained private evidence remains mode `0600`.
There was one capability-driven release re-home, zero hidden approval dialogs, zero wrong-cwd or
stale-artifact acceptance, zero Ivan relay and one writer at each stage. Browser, product runtime,
Supabase, hosted data, provider and deployment validation were correctly omitted because the pilot
changed repository configuration only. The unsupported Prettier parser for `.prettierignore` was
reported as an omission; exact byte comparison and diff hygiene passed instead.
